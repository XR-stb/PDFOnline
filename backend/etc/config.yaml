default:
  port: 8080
  debug: true
  # log_dir:
  static_dir: ./static
  admin_username: admin
  admin_password: admin123
database:
  host: 127.0.0.1
  port: 3306
  user: root
  password: 123456
  db_name: pdf_online
smtp: # optional, if not set, output to console
  host:
  port:
  username:
  password:
  from: